# AI 服务保存反馈与可用性测试

- 保存只保存配置，不隐式发起模型请求。Saved 使用预留的说明字号状态行，位于按钮之后，5 秒自动消失。
- 测试连接仅对已保存配置开放，通过现有主进程 AI JSON client 发起一次合成请求，验证模型与 JSON 输出。不会发送 Skill、Profile、路径或对话；密钥仍由主进程安全存储读取。
- 请求最多 64 输出 token，30 秒超时，无重试，沿用共享单请求限制、响应大小限制及禁止跳转规则。测试可能消耗服务额度，按钮悬浮说明明确提醒。
- 修改配置会撤销当前测试成功提示，需保存后重测。认证、网络、模型或 JSON 失败只报错，不修改配置或触发其他 AI 任务。
- 构建 `f617ad0113a7`，Electron 隔离 Home + 本地假 HTTP 服务验证 920/1180/1440 保存按钮坐标不变、保存零请求、手动测试忙碌反馈及请求内容。截图 `/tmp/agentenv-ai-service-test.png`。
- 定向主进程、renderer 测试及样式、翻译、UI 契约审计通过。没有调用真实计费模型，未执行全量发布测试。
